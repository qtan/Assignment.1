<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Expense_items" >
    

    <ListView
        android:id="@+id/ExpensesItemsTextView"
        android:layout_width="fill_parent"
        android:layout_height="263dp"
        android:layout_weight="1"
        android:scrollbars="vertical" >

    </ListView>

    <Button
        android:id="@+id/AddButton0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/ExpensesItemsTextView"
        android:layout_below="@+id/ExpensesItemsTextView"
        android:layout_marginLeft="23dp"
        android:layout_marginTop="25dp"
        android:text="Add" />

    <Button
        android:id="@+id/EditButton0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/AddButton0"
        android:layout_alignBottom="@+id/AddButton0"
        android:layout_centerHorizontal="true"
        android:text="Edit" />

    <Button
        android:id="@+id/DeleteButton0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/EditButton0"
        android:layout_alignBottom="@+id/EditButton0"
        android:layout_marginLeft="18dp"
        android:layout_toRightOf="@+id/EditButton0"
        android:text="Delete" />

</RelativeLayout>
